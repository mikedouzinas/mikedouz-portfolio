# Overnight Harlequin run — index

> Branch: `feat/harlequin-overnight`. **Not pushed.** Review locally, then approve.
> Generated autonomously overnight. Nothing here is final — code fixes are scoped
> and verifiable; everything in `design-ideas/` and `investigations/` is for you to
> read and decide on. The repo is **public**, so no private vault material lives in
> this tree — personal notes are in `~/Downloads/Dev/_overnight_private/` (outside git).

## How this is organized

- **`investigations/`** — diagnosis + populated-ticket notes for things I did *not*
  auto-complete (need your call). Each file maps to a ticket. The matching GitHub
  ticket bodies were also enriched with a short version.
- **`design-ideas/`** — creative directions for the design-heavy tickets. Multiple
  distinct options per area with tradeoffs, so you can pick — I did not lock a design.
- **`portal-lockups/`** — standalone HTML lockups of the "little window" portal entrance
  (#62). Open the `.html` files directly in a browser. **Not wired into the app.**
- **`SUMMARY.md`** — read this first in the morning: what shipped, what to review,
  and the exact `done --yes` commands for anything ready to close.

## What got auto-fixed (code, in this branch)

See `SUMMARY.md` — each fix is its own commit, each tied to a ticket.
